import React from 'react'
// import PropTypes from 'prop-types'

const Minimal = (props) => {
    return (
        <div>
            
        </div>
    )
}

// Minimal.propTypes = {

// }

export default Minimal